# 特殊建筑物设计 · STRUCTURES

> 状态:现行(v0.8)· 数据源:`scripts/entities/*`、`scripts/world/level_builder.gd`
> 关卡里除地面平台外的一切可交互构件,统一在本文登记;新构件先立项再实现。
> v0.13 增补:§0 组件语义统一 + §5 新增动态构件(开关门 / 限时桥)。

## 0. 构件设计原则

1. **一眼读懂**:构件的形状必须暗示功能 —— 门是取景框,加速门是雪佛龙箭头。
2. **单一职责**:一个构件只改一个属性 / 触发一个事件;组合交给关卡设计。
3. **构成主义外观**:全部直角折线 + 硬投影 + 角色色 / 构成红点缀,无柔光。
4. **组件语义统一**(v0.13 立项;v0.18 分层语义 v3 拍板):一切构件与
   平台一样携带语义组 `layer / faces / who / tags` + 组件编号 `id`
   (v3 定义见 levels.md §7.10:layer ∈ 1..8 八层定值,who 为几何体
   集合、空 = 全员共享)——构件不再有"碰撞特权",门 / 板 / 开关在
   图层系统里与地形同一套通行规则;运行时编译共用该模型(levels.md §7 / Comp)。
5. **外观双轨**(v0.13,art-style.md §6.7):构件的"状态机 + 演出"永远
   引擎侧(LeverGate 切位 / TimedBridge 周期 / PianoTile 脉冲与粒子),
   瓦片只提供**两态静帧外观**(实心 / 虚线框、凸 / 凹),切换合成在引擎;
   碰撞与渲染后端无关。

## 1. 终点门 ExitDoor(`scripts/entities/exit_door.gd`)

| 项 | 内容 |
|---|---|
| 外观 | 几何体专属取景框门:门框 = 目标角色色的双线取景框 + 顶部红色编号刻度 |
| 归属 | 只接受 `geo_index` 对应的几何体;他人可穿过不收取 |
| 状态机 | 待命(角色到站站立)→ 到站(arrived,可撤销)→ 全员到齐 sealed(封印)→ 吸入 |
| 演出 | 到站:角色 0.22s 滑入门中线;激活:依次 0.18s 间隔吸入,缩小至 8% + 粒子 |

## 2. 加速门 SpeedGate(`scripts/entities/speed_gate.gd`)

| 项 | 内容 |
|---|---|
| 外观 | 雪佛龙折角通道(双排箭头指向行进方向),穿越时箭头亮起 |
| 效果 | 速度上限立即抬升至 `buff_sprint_speed`(永久,死亡重生失效);只加速不影响重量 |
| 演出 | 首次触发:HUD 旁白 + buff 音效;速度线残影自然出现(>1.2×) |

## 3. 曲面跳跃板 Ramp(`scripts/world/level_builder.gd` Ramp)

| 项 | 内容 |
|---|---|
| 外观 | 折线曲面(折角步进 ≤7°)+ 硬投影 + 红色刻度块;顶缘亮线 |
| 物理 | 逐段实心凸四边形碰撞;圆球贴坡时速度对齐切线,滑出沿切线飞出 |
| buff | 踩上:速度上限 ×1.5 + 等效重量减半;离开后残留 1.5s;与加速门/冲刺叠乘(取最高再 ×1.5) |

## 4. 移动构件 Mover(v0.8 新增,`level_builder.gd`)

| 项 | 内容 |
|---|---|
| 外观 | 与普通平台同语言的石板条,侧缘带角色色/构成红方向刻度(向哪动刻度在哪侧) |
| 物理 | `AnimatableBody2D`(sync_to_physics)在两点间直线往返:水平或垂直 |
| 参数 | `rect` 尺寸、`offset` 往返向量(px,沿单轴)、`period` 秒、`phase` 相位 |
| 设计值 | 慢速 3.0–4.0s/程(踩着思考)、中速 2.0–2.5s/程(踩着赶路);振幅 100–240px(1–2.4 格) |
| 承载 | 站立者随平台移动(CharacterBody2D 平台速度继承);驮人链条照常工作 |

## 5. 规划中(立项待实现,按优先级)

| 构件 | 概念 | 立项理由 | 依赖 |
|---|---|---|---|
| **开关门 LeverGate**(v0.13 新立 · v0.15 一门多开关) | 场内踩踏开关与门板成对;踩下 ↔ 门板 faces 在 full/none 间切换,门板虚化态保留 8% 亮度线框;**一门可配多只开关(任一踩住即开)**,开关与门之间画地面连线标出归属 | 动态组件首版成员;合作分工新语言——一人踩门一人过;多开关 = **气闸式互让**(门两侧各一只:先过者踩住对侧接留守者),序章 05 机关·合拍首用 | **图层系统**(运行时碰撞位切换,levels.md §7.6) |
| **限时桥 TimedBridge**(v0.13 新立) | 周期性实心 ↔ 虚化(on/off 各 ≥1s 保证可读),虚化期无碰撞、降透明度 | 动态组件首版成员;给"出手时机"再添一个全局钟;可订阅节拍时钟踩在拍点上(audio.md §5) | **图层系统**(同上);节拍联动依赖 BGM 序列器 |
| **钢琴地板砖 PianoTile**(v0.13 新立) | 踩踏/滚过即发声的平台砖;音级 = 格 y 反向映射(高砖 = 高音),全语义组件(天花砖 = faces=bottom 逆的对答区);角色动词 = 演奏手法(圆 = 琶音 / 疾 = 切分 / 跃 = 颤音 / 逆 = 对答 / 踩头 = 和音) | **玩家行为即配乐**(Sound Shapes 路线);锁 C 大调弹不出错音 | **七音符体系**(audio.md §1 note API) |
| 弹射板 Launcher | 踩上瞬弹到固定高度,方向固定向上 | 补足"不可跳角色的垂直移动" | 无 |
| 传送对 Portal | 成对直角门,进 A 出 B,保留速度矢量 | 置换关卡的捷径语言 | 无 |
| **充电桩 ChargingPile**(v0.18 立项) | 双子专属踏板;伍任一半体踩住期间,磁感线边界特性**整体失效**(不再阻挡任何几何体),离开即恢复;桩体常亮紫色,磁界解除瞬间给全局可视反馈 | 给「隔」一个全局开关——磁界从常态墙变成可谈判的资源;触发归属 who={4} ≠ 效果范围(全局),是 v3 归属模型的范例件(levels.md §7.10) | 分层语义 v3(who 集合)+ 磁界(已实装) |
| 单向闸 OneWay | 仅单侧可穿的平台薄板 | 竖直关卡防回头 | 图层系统落地后 = faces=top 的预设笔刷,不再是独立构件 |
| 计时环 TimeRing | 穿过加分 / 加时(肉鸽计时模式) | 肉鸽模式竞速层 | 肉鸽框架 |
| 脆弱块 Crumble | 踩上 0.6s 后碎裂重生 | 高压迫感 | 无 |

> 动态构件设计纪律:**切换必须可预读**——动态的是状态,
> 不是规则;玩家看一眼就能算出下一次实心是什么时候(levels.md §3 推广)。

## 6. 命名与登记规则

- 新构件:实体脚本进 `scripts/entities/`,纯绘制辅助留在 `level_builder.gd` 内部类。
- LevelDef 新字段必须可被 JSON 同构表达(未来数据互通的前置)。
- 构件外观 = 引擎侧 `_draw()` 程序化绘制(art-style.md §6);
  **零贴图,像素素材不入渲染**。
- 本文档同步追加"外观 / 物理 / 演出"三行,缺一不收。
