# 美术风格规范 · ART STYLE

> 状态:现行(v0.8 起) · 构成主义 Constructivism + 极简主义 Minimalism + 动态感
> 本文是视觉决策的唯一权威;`scripts/ui/ui.gd` 是调色板与组件的唯一代码来源。

## 1. 调色板

| 名称 | 色值 | 用途 |
|---|---|---|
| INK `#101216` | 墨色 | 全局背景 |
| INK_2 `#16191F` / INK_3 `#1E222B` | 面板墨 | 面板 / 提亮层 |
| PAPER `#EDEAE0` | 纸白 | 主文本、线条、图标主色 |
| DIM `#8E8D85` | 灰 | 次要文本 |
| RED `#E0492F` | 构成红 | **全局唯一强调色**(编号块、强调条、焦点、刻度) |
| 角色四色 `#E0492F / #E8B33A / #4E86D8 / #E07E2E` | | 仅作功能性点缀(角色徽标、属性条、chips) |
| 石板 `#262B34` / 亮面板 `#313845` | 平台基色 | 关卡静态平台(§4;瓦片素材同源,见 §6) |
| 动板 `#2B3140` / 亮面板 `#3A4254` | 移动板基色 | 与静态石板区分(瓦片素材同源,见 §6) |

用色纪律:一屏只允许一个红色主角(强调条、编号块、或焦点框,不叠加);
角色色永远不作为大面积底色;灰阶层次 ≥ 3 级(INK / INK_2 / DIM)保空间感。

## 2. 形状语言

- **禁止**:圆角(rx > 0)、渐变、投影模糊(高斯/阴影 blur)、贝塞尔曲线、发光贴图。
- **必须**:直角矩形、直线段 / 折线、`stroke-linecap="square"`、`stroke-linejoin="miter"`。
- **投影一律硬边**(v0.19):关卡内的投影由引擎光影实算 ——
  DirectionalLight2D + LightOccluder2D,`shadow_filter = NONE` 天然硬边(§8);
  UI 卡片仍用纯色位移色块(实心暗块)。全项目禁柔影、禁模糊阴影。
- 允许的圆:几何圆环"太阳"、圆球角色、圆点刻度 —— 圆必须是"完整的几何圆",
  不允许椭圆装饰、圆弧飘带。
- 构成主义母题库:红色小方块标记、细线分隔条(rule)、大号数字编号(01/02…)、
  角部刻度、雪佛龙折角、几何圆环、取景框角标、斜切三角 / 梯形巨面。
- 字体:NotoSansSC 可变字重;标题 900(+2 字距)、正文 400、注释 330。
- SVG 素材:仅 flat 单样式;由 `tools/gen_svgs.py` 生成,禁手改;
  ThorVG 不支持 `<text>`,字母用折线字形。

## 3. 动效法则(MOTION RULES)

> 一切动画服务于"构成主义的机械感":像印刷机、像起重机、像节拍器 ——
> 不像果冻、不像水面。动效是让静止海报"通电",而不是把它变成卡通。

| 法则 | 规定 |
|---|---|
| M1 缓动白名单 | 入场 `TRANS_CUBIC/QUINT + EASE_OUT`(硬减速停)、回弹 `TRANS_BACK`、
节奏件 `TRANS_SINE` 往返;**禁** `TRANS_ELASTIC`(果冻感)、`TRANS_BOUNCE`(皮球感,圆球角色物理除外) |
| M2 时长档位 | 微交互 0.10–0.16s;面板出入 0.25–0.45s;场景转场 0.5–0.8s;只允许 0.05s 步进 |
| M3 直线优先 | 位移沿水平 / 垂直轴;斜向入场统一 45° 折线感(先斜插再归位) |
| M4 投影同步 | 关卡投影 = 引擎实算,天然随动;UI 卡片硬投影随本体一起动(整体 tween),不允许投影滞留 |
| M5 呼吸与脉冲 | 常驻动效(呼吸角块、脉冲刻度)周期 1.5–2.5s,幅度 ≤ 10%,永不居中抢焦点 |
| M6 打点音效 | UI 动效 landing 瞬间配 `ui_click / ui_open / ui_page` 等打点音;无声动效不得超过 0.4s |
| M7 逐字 / 逐项入场 | 列表与标题逐项 stagger 0.04–0.07s;方向统一(自上而下或自左而右) |
| M8 屏幕震动 | 仅死亡 / 通关大事件;幅度 ≤ 8px,衰减指数 ≥ 14/s;
M8b 补充档 ≤12px 限巨构大落差坠落 / 复合事件(实机手感回写,见 motion.md §7) |
| M9 帧率独立 | 全部 tween 用秒;常驻脉冲用 `Time.get_ticks_msec()`,禁止帧计数驱动 |

## 4. 关卡视觉层次

1. 背景层(视差 0.06–0.6):巨面 / 山脊 / 星阵,亮度 ≤ PAPER·8% 不与平台争夺注意。
2. 平台层:石板 `#262B34` + 亮面板 `#313845` + 顶缘线 PAPER·30% + 红色刻度块。
3. 实体层:角色 / 门 / 门牌(高饱和)。
4. UI 层:细线 + 色块 + 大字。

## 5. 风格审计清单(每次大改后过一遍)

- [ ] 全部 StyleBoxFlat `corner_radius == 0`
- [ ] 无 `Gradient` / `GradientTexture2D` 用于装饰(UI 的 radial_tex 仅限历史兼容,新代码禁用)
- [ ] 阴影只有硬边:关卡 = 引擎光影实算(§8);UI = 纯色位移色块;零模糊阴影
- [ ] SVG 素材全部 flat 单色、直角折线
- [ ] 动效缓动在 M1 白名单内;时长符合 M2 档位
- [ ] 红色使用点每屏 ≤ 3 处角色(强调、刻度、焦点各一)
- [ ] 图标含 `<text>` 的为 0(ThorVG 限制)
- [ ] 地图渲染两轨:默认全 `_draw()` 程序化;测试关可挂 aseprite 地图皮
	  MapSkin(§6.2,唯一贴图地形管线);除此之外无游离的像素素材引用
- [ ] aseprite 例外两处 = 档案几何插图(§6.1,仅 assets/archive/)+
	  关卡地图皮(§6.2,仅 assets/levels/,PNG 必须半分辨率);
	  地图与实体不得引用其余像素素材

## 6. 美术素材规范 · MAP ART(v0.29 修订:程序化为默认 + MapSkin 测试关例外)

> 状态:**现行(v0.29)**。默认管线仍是 `_draw()` 程序化绘制(2026-09-09
> 用户决策);v0.27 起"测试关美术层"以 **MapSkin 地图皮**形式回归为
> **唯一贴图地形例外**(用户指令"测试关全部重制,地图采用 aseprite 绘制"),
> v0.29 把契约成文并修复双倍放大遗留 bug。档案插图例外见 §6.1,
> 地图皮契约见 §6.2。

### 6.1 唯一例外:档案几何插图(v0.19 立)

- **范围**:仅「档案几何」(ArchivePanel)页内的图鉴示例图使用
  aseprite 素材;地图 / 实体渲染仍全 `_draw()`,本节不构成管线重启。
- **管线**:源 `assets/art/tiles_v2/*.aseprite`(bld_* 建筑 / mech_* 机关 /
  geo_* 几何肖像,清单见其 README)→ 导出 1x PNG 到 `assets/archive/`
  (唯一入引擎的 aseprite 衍生素材目录)→ ArchivePanel 加载。
- **统一画布 200×200**(= 2×2 格,1 格 = 100px):几何肖像按本体 ×2
  放大绘制,保持形体比例可比;调色板与 §1 完全同源;图层纪律
  body(基色 + 内缘自阴影)/ panel(顶部受光)/ edge(纸白顶缘)/
  motif(白色主纹 + 墨色印刷错位)/ guide(隐藏参考框);
  **几何肖像不画硬投影、不加底部暗带(v0.19.2/3 用户决策)**,
  墨影错位一律收在形体轮廓内;土建构件瓦片仍按 shadow +7/+8 规范。
- **显示纪律**:详情图 2× 整数放大 + `TEXTURE_FILTER_NEAREST`;
  列表缩略图精确 1/4(50×50);禁止非整数缩放与柔化滤镜。
- **动态帧**(v0.19.0):动态构件 / 规划构件用 aseprite 多帧精灵循环
  (终点门 待命→到站→吸入 3 帧、限时桥 实↔虚 2 帧、传送对 3 帧),
  导出后缀 _f2/_f3;图鉴以 Timer 按条目周期播放,静态两态机关仍为
  手动切换的静帧。

### 6.2 关卡地图皮 · MAPSKIN(v0.29.1 契约修订:全分辨率;v0.27 首例)

- **定位**:测试关美术层例外——`LevelDef.art` 非空时,aseprite 地图皮
  接管**地形外观**,`LaneRenderer` 让位;碰撞照走平台组件,机关物 /
  玩家仍引擎 `_draw()`(structures.md §0.5 双轨之测试关例外)。
  仅用于机制试炼场;正式关卡是否启用待机制期后另行决策。
- **管线与尺寸契约(严格)**:`assets/art/levels/<level>.aseprite` 源
  → 导出 PNG(1x)至 `assets/levels/<level>.png` → 引擎 Sprite2D
  (`scale ×1`,`TEXTURE_FILTER_NEAREST`,`z=-1` 沉到网格下)。
  **PNG = 全分辨率 = 关卡世界尺寸 1:1**(1 aseprite 像素 = 1 引擎像素;
  v0.29.1 起细化升级,画布 6400×1080)。沿革:v0.27 全分辨率 ×
  `scale ×2` = 双倍放大错位(2026-09-12 探针帧实证);v0.29 曾改半
  分辨率 ×2;v0.29.1 定格**全分辨率 ×1**,源画布即引擎所见,
  不允许任何导出缩放。换 PNG 内容后必须重跑 `--headless --import`。
- **画布层栈(自底向上,首例 trial_v5;v0.29.1 起细节规范)**:
  1. `bg_deep` — 墨底 + 硬边值阶横带(禁渐变,用值阶做氛围梯度;
	 目标区可整体提亮一至两档,如 Z5 归门光柱 + 1px 竖缘线);
  2. `bg_towers` — 远景退台巨塔剪影(INK_2 体 + INK_3 顶缘/左缘受光
	 /窗槽双排(少量 LIT2 微亮"住人"窗)+ 桅杆信标(塔高 ≥320),
	 与 Backdrop 折线山脊同档,不得更高);
  3. `bg_mid` — 世界线构:管线(2px + 法兰 + 吊杆)/ 桁架(双弦 +
	 立杆 + 斜腹 + 节点板,折线禁弧)/ 区段纹样(雪原折线 2px /
	 推箱导轨双线 + 枕木)/ 传送菱标 / 地标环(完整几何圆,允许;
	 圣环 = 双环 2px+1px + 12 径向刻度);
  4. `terrain` — 石板本体 `#262B34` + 内缘受光带 `#313845`(6px)+
	 受光过渡缝(1px INK_2)+ 板缝(2×10 沟槽,480 节奏)+ 板角
	 铆钉(3×3)+ 墙基接缝 + 悬浮件 45° 裙角,**逐像素对齐碰撞矩形**;
  5. `edge` — 可站缘纸白 30%(2px,合成 ≈`#626467`;坑壁竖缘 /
	 角部回包 12px)/ 逆可站天花底缘逆蓝 30%(2px,≈`#324665`);
  6. `accent` — 红刻度节奏(6×4,每 480 引擎 px 一处)+ 坑肩警示
	 (4×8)+ 站台单刻 + 出生倒三角;
  7. `fx` — 星阵三档(微点 / 方点 / 十字闪点,低亮不与平台争夺);
  8. `guide` — 隐藏层:碰撞框参考(**导出前确认不可见**——
	 2026-09-12 实测导出会合成可见层,漏关即污染成品)。
- **绘制禁区**:不画机关物 / 玩家 / 磁界 / 网格 / 分区标注 / 教学牌
  (全部引擎负责);不写文字(ThorVG 折线字形纪律同源);无圆角、
  无渐变、无模糊投影(§2 全局适用)。
- **双端**:mobile renderer 实测通过(桌面);真机 K60 走查随下次
  打包顺带。全分辨率 1:1 + NEAREST 下无缩放伪影;相机远景 LOD
  (zoom <1)为轻微缩小采样,属像素画常规表现。
- **首例**:`assets/art/levels/trial_v5.aseprite`「长卷 · 归门圣环」
  (v0.29 重绘;v0.29.1 升全分辨率 6400×1080 并细化:双排窗槽 /
  信标 / 桁架节点 / 导轨枕木 / 圣环径向刻度 / 板缝铆钉,
  参考系转译见 §9)。

- **地图渲染默认 = 引擎侧 `_draw()`(非 MapSkin 关卡的唯一管线)**:
  平台 / 曲面 / 移动构件 / 动态构件 / 网格全部由
  `level_builder.gd` 程序化绘制;
  石板 `#262B34` / 亮面板 `#313845` / 动板 `#2B3140·#3A4254`、
  顶缘线 PAPER·30%(动板 PAPER·42%)、左缘红刻度块每 480px 一处;
  立块底缘 45° 裙角保留;**投影不再手绘**(v0.19 撤硬投影暗块),
  由关卡根节点的引擎光影 rig 实算(参数见 §8)。
- **语义视觉变体(v0.18 分层语义 v3 八层定值,levels.md §7.10)**:
  景观层深度梯度——L3 背景压亮度 ×0.45(背景红线内)/ L2 远景冷色
  (0.62,0.66,0.78)基础透明 0.34 / L1 深景(0.42,0.46,0.58)透明 0.26,
  均在定位网格之下;L8 前景 z=6,玩家躲入其后降至 55% 呈剪影;
  实体层(L4–L7)高亮三档——专属(who 含受控者)按受控几何体色
  描边呼吸脉冲 / 共享(who 空)常亮 / 无关幽灵 35%;切换几何体时
  按距离波次级联、交叉淡化(近先远后 ≤0.3s);
  faces=top 顶缘亮线加亮、bottom 底缘蓝色细线(逆 = 蓝)、none 8% 线框;
  动态构件虚化态 8% 亮度线框 + 虚线段。全部引擎 modulate / 色彩实现,
  无任何贴图。
- **图标 / 角色 SVG 素材不受影响**:`assets/svg/`(flat 单样式,
  `tools/gen_svgs.py` 生成)继续用于 UI 图标与角色徽标。
- **形状与调色板纪律(§1/§2)照旧全局适用**,包括程序化绘制:
  无圆角、无渐变、无模糊投影、直角折线。

## 7. 剧幕主题差分与统一(v0.16 定稿,治理"多页面不统一太杂乱")

> 大主题锚定不变:**构成主义 + 几何棱角分明 + 极简主义**。差分不碰地基——
> 调色板(§1)、形状语言(§2)、动效法则(§3)全章适用每一幕。

- **差分只许动三样**(关卡世界层):
  1. 背景巨面构图与亮度节奏(视差层的"海报排版");
  2. 刻度 / 点缀的角色色密度(疾关红密、跃关黄点缀,levels.md §4 既有);
  3. 结构尺度与排布密度(巨构 = 3× 尺度;极简 = 减法留白)。
- **统一纪律**:UI 层(菜单 / 面板 / HUD / 档案)全局一套,**不随幕变**——
  页面越多的系统越要一张脸;幕差分只发生在关卡世界层。
- **各幕主题表**(叙事对位见 story.md §1.5):

| 幕 | 主题 | 关键词 |
|---|---|---|
| 序幕 | 构成主义(基调) | 红色刻度、细线、星阵 |
| 第一幕 | 巨构主义 | 3× 巨面、门厅桥柱梁、分位 |
| 第二幕 | 极简主义 | 减法、留白即道路、磁力线独色 |
| 第三幕 | 梦核主义(例外章) | 唯一允许柔光与微曲线的幕(例外清单显式登记) |
| 第四幕 | 构成 + 角色色强化 | 蜕变:每人一个色面高潮 |
| 第五幕 | 巨构主义 · 终演 | 全主题要素回收同屏 |
| 落幕 | 极简回白 | 回到序幕的白 |

- **梦核幕例外清单**(进幕前登记,退幕即失效):允许柔光边、微曲线装饰;
  仍禁止:渐变大面积底、圆角控件、模糊投影。例外清单外的违例照常审计。

## 8. 光影与阴影 · 引擎内置光影实装(v0.19)

> 状态:**现行(v0.19)**。2026-09-10 用户决策:手绘的"伪装影子"(实体系脚下
> 椭圆投影 + 建筑/机关整体位移硬投影暗块)观感假,全部撤下;改用 Godot
> 内置光影实算。v0.16 的"P2 观望"评估就此结案,**推翻**其中"脚下投影保留
> 手绘"一条——落点预判信息随伪影一并移除,由投影方向与关卡结构自身表达。

- **亮度模型**:`最终色 = 本体色 ×(环境档 + 光照档)`。
  受光区 ≈ AMBIENT + SUN ≈ 1.0(色板还原,附轻微冷暖分离:环境冷档、
  光暖纸白);阴影区只剩 AMBIENT(压暗 ≈30%)。
- **rig**(LevelBuilder.build() 装配,参数常量在文件头):
  `CanvasModulate(LIGHT_AMBIENT 冷色压暗)` + `DirectionalLight2D`
  (rotation ≈ -40°,光来自左上 → 影子投向右下,沿袭旧硬投影 offset(7,8)
  的方向约定;`shadow_filter = NONE` 硬边,符合禁模糊纪律)。
- **遮挡体(LightOccluder2D,顺时针绕行 + `CULL_CLOCKWISE` 防自投影)**:
  平台(L3 起可见实体件,faces ≠ none;L1/L2 雾化剪影不挂,避免全强度
  硬影与档位失配)/ 曲面板(与绘制主体同形)/ 动板(随动)/ 限时桥与
  开关门板(随实/虚切换,虚化不投影)/ 钢琴砖 / 几何体本体(形体即影子,
  多边形与碰撞形一致,随挤压与进门缩小同步,死亡淡出即收)。
- **演出语义**:投影随本体天然随动(M4);方向全屏恒定;动态构件虚化态
  不投影,与线框虚化语言一致(所见即所影)。
- **待办/风险**:移动端全屏光照 pass 开销按"测量先行"纪律上真机 profile;
  若吃紧,先调大 `DirectionalLight2D.max_distance` 剔远景阴影或降遮挡体
  数量,不动风格参数。

## 9. 参考系转译 · JOURNEY / GRIS / THOMAS WAS ALONE(v0.29 登记)

> MapSkin v2 绘制前做的三作风格调研结论,登记为**后续 aseprite 素材的
> 转译依据**:只借"氛围构造法",不借色板与柔质;冲突处一律以 §1/§2 为准。

| 来源 | 值得借的构造法 | 转译为本项目(§1/§2 约束下) | 明确不借 |
|---|---|---|---|
| **Journey**(风之旅人)| 分层剪影景深(远山/中景/近景三级互不争抢);目的地"圣光"叙事——终点在构图与亮度上被预支;沙丘式大留白与横向长卷节奏 | 值阶分层剪影(bg_towers 塔群 ≤ INK_3,越远越暗);归门 = 圣环 + 值阶光柱(Z5 提前可见);长条关卡按"节拍"分配纹样密度 | 柔光辉光、有机曲线沙丘(→ 硬边值阶 + 45° 折线) |
| **GRIS**(灰色)| 色彩即情绪章节(幕 = 色彩阶段);同一场景内用极克制的变化累积情绪 | 与既有七幕主题差分(§7)同构,此处互为印证;单幕内部用**硬边值阶**(每档一个灰阶色,禁渐变)做情绪梯度 | 水彩晕染、大面积渐变底(→ 海报式分层) |
| **Thomas Was Alone**(孤独的托马斯)| 深底亮缘的极致可读性:角色永远是全场最饱和的形;抽象几何靠动效与台词获得人格;灰箱关卡即美术 | 墨底 + 地形顶缘纸白线(角色四色在暗场上唯一高饱和);几何体人格已由 motion grammar 承担,皮肤保持安静 | 角色发光描边(→ 引擎光影 + 色彩纪律即可) |

- **统一纪律重申**:三作的共同成功点是"少而一致",与本仓 §7 幕差分
  "统一不歧化"同源;任何参考系引入的新母题(圣环 / 光柱 / 值阶带)
  须登记进 §6.2 层栈清单后才可在 MapSkin 中复用。
